import { BrowserRouter as Router, Switch, Route, Router } from "react-router-dom";

export const route = () => {
    return(
        <Router>
            <Switch>
                <Route path = "/">

                </Route>
            </Switch>
        </Router>
    )
}